import { Route } from '@angular/router';
import { RegistrarComponent } from './registrar.component';

export const RegistrarRoutes: Route[] = [
    {
        path: 'registrar',
        component: RegistrarComponent
    }
];