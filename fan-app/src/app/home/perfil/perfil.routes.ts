import { Route } from '@angular/router';
import {  PerfilComponent } from './perfil.component';

export const PerfiRoutes: Route[] = [
    {
        path: 'perfil',
        component: PerfilComponent
    }
];